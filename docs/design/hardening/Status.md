# Proxy Hardening — Test-Type Coverage Status

**Revision:** 6
**Last modified:** 2026-07-02T12:57:04Z
**Status:** In progress — 10 test types PASS with captured live evidence (unit [control-plane, all pkgs pass], stress+chaos, DDoS, concurrency/atomicity, memory-soak, **P10 VPN fail-closed full-automation GREEN**, **race/deadlock [0 DATA RACE]**, **benchmarking/performance [data-plane p50=86ms + control-plane Go micro-benchmarks]**, **Challenges [2 PASS / 1 honest SKIP]**, **security ACL live-deny GREEN [authoritative Squid access.log `TCP_DENIED/403` + `HIER_NONE` — deny enforced + no upstream leak; S3 Via + S4 SOCKS-SSRF also PASS, wired into the standing suite §11.4.135]**), 1 honest SKIP (HelixQA — bank blocked on 6 un-vendored siblings), integration + e2e PENDING (broader `tests/run-tests.sh` suite). The control-plane store-layer audit-atomicity TOCTOU was investigated this round and found already CLOSED (`store.WithTx`), verified RED→GREEN. No PASS is claimed without a real `qa-results/` evidence path (§11.4.6).
**Authority:** Inherits `constitution/Constitution.md` per §11.4.35. §11.4.45 integration-status doc reconciling the §11.4.169 test-type-coverage matrix for the proxy hardening workstream.
**Companion:** summary [`Status_Summary.md`](Status_Summary.md)

## Operator-blocked / pending items (read first — §11.4.45 O(1) surface)

| Item | Why blocked / pending | Unblock condition |
|---|---|---|
| P10 real-VPN egress-half (SKIP) | The egress half — proving traffic actually EXITS via the tunnel — needs operator gluetun WireGuard credentials (§11.4.21). The fail-closed half is now GREEN. | Operator provides gluetun WireGuard creds; then the real-egress positive path can be captured. |

## §11.4.169 test-type-coverage matrix (captured-evidence-driven — §11.4.5/§11.4.69)

Base proxy stack UP on `:53128` at capture time. Every PASS below cites a real, non-empty `qa-results/` artefact captured live this session.

| Test type | Status | Evidence path | Notes |
|---|---|---|---|
| Unit | PASS | `qa-results/unit/control-plane_unit_20260701T131306Z.log` | `go test -mod=readonly -count=1 -cover ./...` on `control-plane`: all packages PASS, 0 failures. Coverage: aclhelper 100%, breaker 97.5%, pac 93.3%, routing 85.9%, redis 81.2%, vpn 78.6%, compiler 71.2%, api 69.4%, store 64.5%, healthd 61.3% (cmd shims / otel = integration-only). |
| Integration | PASS | `qa-results/integration/control-plane_verbose_20260702T125403Z/integration_verbose.log` | `go test -run Integration -v -count=1 ./...` on control-plane against REAL podman infra (#69): **14 PASS / 1 honest §11.4.3 SKIP / 0 FAIL**. Real-Postgres: `TestIntegration_API_CRUD_RealPostgres` (7.5s) + 6× `internal/store` (`ProfilesCRUD`/`TargetsTiersRules`/`UsersAndAudit`/`WithTxAtomicAuditAndMutation`/`NoSQLInjection`, 32.9s). Real-Redis: 4× `internal/redis` (`StatusRoundTrip`/`TTLExpiryIsFailClosed`/`RouteRoundTrip`/`PubSub`) + compiler `RunSeedsRedisAndWritesArtifacts` (8.4s). Real-gluetun: healthd `HealthdWritesDownAgainstRealGluetun` (6.0s). routing `CompileAndSquidParse` (6.6s); api metrics-listener graceful-cancel. Real-infra (§11.4.27 — tests named `*RealPostgres`/`*RealRedis`, unique throwaway podman containers; the aardvark-dns break is compose-network-only, NOT ad-hoc `podman run`). The 1 SKIP = acl-helper `RealHelperRealRedis` (needs EXTERNAL `REDIS_ADDR`, honest §11.4.3). §11.4.6 correction: a prior round had marked this PENDING assuming podman was unusable — the captured `-v` verdicts prove it runs. |
| E2E | SKIP (§11.4.3) | — | No dedicated control-plane e2e suite exists in-project (only submodule e2e under docs_chain/containers/constitution). End-to-end behaviour is covered by the **Full-automation (P10 VPN fail-closed)** row below (PASS — data-plane tunnel-DOWN→branded-503) + the **Integration** row above (real API→Postgres→redis→squid-config path, 14 PASS). A full compose-network-wired data-plane stack e2e is operator-gated — the aardvark-dns podman compose network (binds :53) is broken → honest SKIP, NOT a fake PASS. A dedicated control-plane e2e harness is tracked NEW work, not a hardening-round regression (#69). |
| Full-automation (P10 VPN fail-closed) | PASS | `qa-results/dynamic/vpn_failclosed/20260701T130115Z/verdict.txt` (+ `req_1..3.body` branded pages) | `tests/dynamic/vpn_failclosed_test.sh` GREEN live on the booted dynamic stack: tunnel DOWN ⇒ branded 503 `ERR_TUNNEL_DOWN` ×3 (3132-byte real page, 7 brand markers), `leak_seen=0`, `upstream_forward_lines=0`, Squid PID unchanged (36). Deterministic ×3 (§11.4.50), RED polarity guard FAILs a fabricated 200 leak (§11.4.115). Egress-half operator-gated SKIP (§11.4.21). |
| Challenges | PASS (2/3, 1 honest SKIP) | `qa-results/challenges/20260701T131440Z/summary.txt` | `challenges/scripts/run_proxy_challenges.sh` live vs base proxy: HTTP-forward **PASS** (`Via` header + through-vs-direct), SOCKS5 **PASS** (204 + HTTPS 200 via `:51080`), cache **SKIP** (access.log not readable — client-side `Via`/`Age` captured as supplementary, no faked `TCP_HIT`, §11.4.3). Tally `PASS=2 FAIL=0 SKIP=1`, `RESULT: OK`. |
| HelixQA | SKIP (blocked) | `qa-results/helixqa/20260701T090532Z/` (honest SKIP path) | Honest §11.4.3 SKIP: the `helixqa` binary won't build here — `submodules/helix_qa/go.mod` `replace`s 6 own-org siblings not vendored (`doc_processor`, `llm_orchestrator`, `llm_provider`, `llms_verifier`, `vision_engine`, `security`). Proxy bank `tools/helixqa/runner/run_proxy_bank.sh` (6 cases PRX-*) + `proxy.yaml` present; unblock = vendor the 6 siblings or pass `HELIXQA_BIN`. Not a fake PASS. |
| DDoS / load-flood | PASS | `qa-results/ddos/proxy_flood_20260701T122320Z/flood.evidence` (+ `census.txt`) | 300/300 requests landed (`success=300 refuse_shed=0 timeout=0 error=0`); HTTP `:53128` + SOCKS5 `:51080` still listening after flood; recovery code 204. `OVERALL=PASS`. **Go control-plane (gap closed #67, `0c51f61`, §11.4.6):** the row above is DATA-PLANE Squid — the Go control-plane server had no DDoS coverage. Now `internal/api/loadflood_test.go` (8-worker, 240 mixed PUT/GET/PAC/SSE over the in-process httptest harness — no infra) + `internal/aclhelper/decide_loadflood_test.go` (20k seq + 40k concurrent `Decide`): calibrated-per-run p99 tail + NumGoroutine/`/proc/self/fd` leak census, Type-4-torn-read fold, §1.1-load-bearing (independent §11.4.142 review a02f558f GO). Evidence `qa-results/ddos/control-plane_<id>/latency.tsv`. |
| Security | PASS | `qa-results/security/proxy_acl_20260701T152442Z/s1_acl_deny.evidence` | S1 ACL-deny GREEN via the **authoritative Squid access.log** discriminator: a must-deny `CONNECT example.com:80` (non-SSL port, blocked by `http_access deny CONNECT !SSL_ports`) logs `TCP_DENIED/403 ... HIER_NONE` — deny enforced AND no upstream contacted (no leak, §11.4.68/§11.4.69). Client code (000) is supplementary; the log is the oracle (previously the `000` ambiguity forced a SKIP). RED teeth (§11.4.115): an allowed `:443` CONNECT logs `TCP_TUNNEL/200 ... HIER_DIRECT` → S1 correctly refuses a false deny-PASS. S3 Via/version-hygiene + S4 SOCKS-SSRF also PASS; all wired into the standing suite (`run-tests.sh test_security_guards`, §11.4.135). **F1 fix+guard (#71 `6e3e031` / #72 `adca02e`, §11.4.118 discovery-audit):** the group-verdict aggregate was OVER-CLAIMING — a non-critical S3-only PASS reported the group GREEN *naming S4 "GREEN live"* while the critical S1(ACL-deny)/S4(SOCKS-SSRF) SKIPped (plausibly LIVE on the broken-podman host). Fixed so group PASS REQUIRES S1&&S4 to actually pass (else SKIP naming the absent one; run-tests headline honest) + a §11.4.135 standing regression guard (single-source `tests/lib/acl_group_verdict.sh` + `tests/regression/security_group_critical_gate_test.sh`, §11.4.115 RED_MODE polarity, adversarial-revert-proven load-bearing; reviews aefcdb5d + a5ff11ac GO). |
| Stress + Chaos | PASS | `qa-results/stress/proxy_forward_20260701T120843Z/stress.evidence` · `qa-results/chaos/proxy_restart_20260701T120941Z/recovery_trace.log` | Stress: 110/110 forward probes OK (100 sequential + 10 concurrent), `via` header corroborates bytes transited proxy→squid, `OVERALL=PASS`. Chaos: restart-fault injected, recovered code 204 within timeout, `OVERALL=PASS`. **Go control-plane chaos (gap closed #67, `159fcbc`, §11.4.6 — the row above is DATA-PLANE Squid):** `internal/api/chaos_test.go` (store-drop-mid-`WithTx` rollback via `setAuditErr` — 500 + no-partial-state + recovery; SSE-disconnect forwarder-reap; ctx-cancel, real-PG in-flight-abort honest §11.4.3 SKIP) + `internal/aclhelper/decide_chaos_test.go` (redis-unavailable sustained outage → ZERO `(true,*)` leaks across 800 decisions/phase; restore→affirmative returns). §1.1-load-bearing (swallow-audit-err→store-drop FAIL; fail-open Decide→800/800-leak FAIL); independent §11.4.142 review a139f9ec GO. Evidence `qa-results/chaos/control-plane_<id>/`. |
| Concurrency / atomicity | PASS | `qa-results/concurrency/proxy_concurrency_20260701T122740Z/concurrency.evidence` (+ `clients.tsv`) · `qa-results/unit/control-plane_toctou_atomicity_20260701T131522Z.log` | Data-plane: 40 simultaneous mixed clients (http=20 + socks5=20), `crosstalk=0 proxy_drop=0 endpoint_limit=0`, `OVERALL=PASS`. Control-plane audit-atomicity: the store-layer TOCTOU (mutation commits while `AppendAudit` fails) is **CLOSED** — `handlers.mutateWithAudit` wraps mutation+audit in one `store.WithTx` (Postgres BeginTx→Rollback), verified RED→GREEN by `TestAtomicity_MutationAndAuditCommitTogether` (§11.4.115 polarity: GREEN PASS / RED FAIL). |
| Race / deadlock | PASS | `qa-results/race/control-plane_race_20260701T125739Z.log` | `go test -race -count=1 ./...` on `control-plane` (11 pkgs incl. cmd/acl-helper, cmd/healthd, cmd/api, cmd/compiler, internal/{aclhelper,api,breaker,otel,pac,redis,routing,store,vpn}): EXIT=0, **0 `WARNING: DATA RACE`**. Real concurrent tests spawn goroutines against mutex-guarded shared state (`internal/api/server.go:34`, `internal/breaker/tunnelbreaker.go:107`, `cmd/healthd/main.go:263`). The store-layer audit-atomicity gap that was investigated this round is already CLOSED + verified (see Concurrency/atomicity row). |
| Memory | PASS | `qa-results/memory/proxy_soak_20260701T122643Z/soak.evidence` (+ `rss_timeseries.tsv`) | RSS-bounded soak: baseline 35 830 000 B → final 35 890 000 B, `growth_ratio=1.0017` (bound 1.5). `OVERALL=PASS`. **Go control-plane soak (gap closed #67, `ceb4839`, §11.4.6 — the row above is DATA-PLANE Squid):** `internal/api/soak_test.go` churns PUT/GET/PAC/SSE over the in-process harness; HeapObjects/HeapAlloc growth_ratio N-invariant (1.11–1.13 @ N=1500 and N=5000, bound 1.5) + NumGoroutine reap census; found+fixed a fake-audit-accumulation false-fail (`drainFakeAudit`, production persists audits to Postgres); §1.1-load-bearing (leaked goroutine→growth>1.5 FAIL); independent §11.4.142 review a74e23fd GO. Evidence `qa-results/memory/control-plane_<id>/`. |
| Benchmarking / performance | PASS | `qa-results/benchmark/proxy_forward_20260701T130414Z/latency.txt` · `qa-results/benchmark/control-plane_gobench_20260701T131745Z.log` | Data-plane: `tests/benchmark/proxy_forward_benchmark.sh` live vs base proxy — 200/200 proxied 204s, `p50=0.086s p95=0.088s p99=0.091s` (mean 0.087), 10.841 req/s, `OVERALL=PASS`. Control-plane Go micro-benchmarks (gap now closed — 3 new `*_bench_test.go`): `Decide` 61–70 ns/op (0 allocs), `SelectTunnel` 30–101 ns/op (0 allocs), `CompileAll` ~39 µs/op (167 allocs), `ResolveTunnels` ~5.7 µs/op. |

## Honest boundary (§11.4.6)

This matrix reconciles the **hardening** test-type picture proven in this session. Ten types (unit, stress+chaos, DDoS, concurrency/atomicity, memory, P10 fail-closed full-automation, race/deadlock, benchmarking, Challenges, security ACL live-deny) are PASS with captured live `qa-results/` evidence. The security-ACL row moved SKIP→PASS this round: the `000` client-code ambiguity that previously forced the SKIP was replaced by reading Squid's own access.log (`TCP_DENIED/403` + `HIER_NONE`), the authoritative sink-side oracle (§11.4.69), with a RED teeth-check (allowed `:443` → `TCP_TUNNEL`/`HIER_DIRECT` → no false deny-PASS, §11.4.115) and the guard wired into the standing suite (§11.4.135). One SKIP is honest (§11.4.3): HelixQA (bank blocked on 6 un-vendored own-org siblings — the runner + `proxy.yaml` bank exist; unblock = vendor them or pass `HELIXQA_BIN`). Integration + e2e remain PENDING — not claims the capability is broken; covered by the broader `tests/run-tests.sh` suite. The two follow-ups flagged earlier this round are RESOLVED: the store-layer audit-atomicity TOCTOU was found already CLOSED (`store.WithTx`, verified RED→GREEN), and the control-plane Go-benchmark gap is closed (3 new `*_bench_test.go`). None of this substitutes for the §11.4.40 full-suite retest before a release tag.
