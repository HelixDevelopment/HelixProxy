# Mullvad WireGuard Egress — Feature Status

**Revision:** 1
**Last modified:** 2026-07-02T20:20:00Z
**Status:** In progress — **the persistent one-device Mullvad WireGuard egress feature is PROVEN this session.** A persistent single-device Mullvad WireGuard identity (device **"groovy rabbit"**, id `df4184d4-b6e9-4c2e-b7f7-16e15d4e55a2`) was registered via the Mullvad app API and its gluetun `mullvad`-native config stored in the **gitignored `.env`** (never in git — §11.4.10). Two security-critical guarantees are captured with live physical evidence: (1) **live egress** — `mullvad_exit_ip=true`, DETERMINISTIC 3/3 (§11.4.50); (2) **kill-switch / fail-closed** — tunnel-DOWN ⇒ ALL egress BLOCKED, DETERMINISTIC 3/3 (§11.4.68). **Honest boundary (§11.4.6):** these prove the Mullvad egress identity + a live tunnel + its kill-switch through gluetun; the full proxy-data-plane-THROUGH-gluetun end-to-end path (real proxy traffic egressing via Mullvad) is still OWED/PENDING, and live DNS is operator-deferred. No PASS is claimed without a real, non-empty `qa-results/` evidence path (§11.4.6).
**Authority:** Inherits `constitution/Constitution.md` per §11.4.35. §11.4.45 integration-status doc for the Mullvad WireGuard egress feature.
**Companion:** summary [`Status_Summary.md`](Status_Summary.md)

## Operator-blocked / pending items (read first — §11.4.45 O(1) surface)

| Item | Status | Why blocked / pending | Unblock condition |
|---|---|---|---|
| Full proxy-data-plane-through-gluetun e2e | PENDING | The egress identity + live tunnel + kill-switch are PROVEN, but wiring the full dynamic proxy stack data-plane THROUGH gluetun (proxy → tunnel → Mullvad exit) end-to-end is the next data-plane step; needs the dynamic stack up. No evidence is cited for this row (§11.4.6 — not yet captured). | Dynamic proxy stack booted with the data-plane routed through the gluetun tunnel + captured live proof of proxy traffic egressing via Mullvad. |
| Live DNS (LE Phase 4/6) | OPERATOR-BLOCKED | Operator-deferred DNS on 2026-07-02 (Let's Encrypt Phase 4 of 6). Not an egress-feature defect — a downstream operator decision. | Operator lifts the DNS deferral. |

## §11.4.45 captured-evidence status table (§11.4.5 / §11.4.69)

Every PASS below cites a real, non-empty `qa-results/` artefact captured live this session. Secrets never appear here — only the device name/id + evidence paths (§11.4.10).

| Aspect | Status | Evidence path | Notes |
|---|---|---|---|
| Persistent one-device WireGuard identity | PASS | `qa-results/verification/mullvad_egress_20260702T161312Z/PROOF.txt` | A persistent single-device Mullvad WireGuard identity — device **"groovy rabbit"**, id `df4184d4-b6e9-4c2e-b7f7-16e15d4e55a2` — was registered via the Mullvad app API. "One same device" is guaranteed by reusing the persistent private key across every run. Credentials (`VPN_SERVICE_PROVIDER=mullvad` + WireGuard key + assigned address) live ONLY in the gitignored `.env` (§11.4.10 — never in git). |
| Live Mullvad egress (`mullvad_exit_ip=true`) | PASS | `qa-results/verification/mullvad_egress_20260702T161312Z/PROOF.txt` (+ `egress.json`, `egress_iter2.json`, `egress_iter3.json`) | gluetun (rootless podman, §11.4.161) brought up a kernelspace WireGuard tunnel and `https://am.i.mullvad.net/json` returned `mullvad_exit_ip=true`. DETERMINISTIC 3/3 (§11.4.50): the invariant `mullvad_exit_ip=true` held on every run (exits Prague, then Dusseldorf, then San Jose — the exit city varies by gluetun relay rotation, but the persistent device "groovy rabbit" ALWAYS egresses via Mullvad). Host routing untouched (§11.4.174); all verification containers torn down (§11.4.14). |
| Kill-switch / fail-closed (tunnel-DOWN ⇒ egress BLOCKED) | PASS (§11.4.68) | `qa-results/mullvad_killswitch/20260702T165417Z/08_killswitch_loop.txt` (+ `SUMMARY.md`) | gluetun `FIREWALL=on`. Atomic per-iteration: tun0 brought DOWN, confirmed DOWN before AND after each probe, raw-IP egress attempted (DNS-independent, 2s window inside gluetun's healthcheck restart cycle). Tunnel DOWN ⇒ every raw-IP egress FAILED (GNU wget `RC=4` = network failure from the firewall DROP). DETERMINISTIC 3/3 (§11.4.50). The one exploratory probe that succeeded ~8s post-drop did so via gluetun's AUTO-RESTORED tunnel (a new Mullvad exit) — verified against gluetun restart logs, NOT a leak (`07_drop_blocked.txt` annotates this; the definitive proof is the atomic N=3 loop). |
| Kill-switch firewall mechanism (iptables) | PASS | `qa-results/mullvad_killswitch/20260702T165417Z/04_iptables_up.txt` | Proof the block IS the firewall: iptables `OUTPUT` policy = `DROP`; the only general-egress ACCEPT rule = `-A OUTPUT -o tun0 -j ACCEPT` (non-tunnel egress limited to loopback, LAN, and the VPN endpoint). So when tun0 is DOWN there is no permitted egress path — fail-closed by construction. |
| gluetun healthcheck corroboration | PASS | `qa-results/mullvad_killswitch/20260702T165417Z/09_gluetun_firewall_block_evidence.txt` | Independent, unforgeable corroboration: while tun0 was down, gluetun's OWN healthcheck probed raw DNS IPs from the physical interface and every attempt returned `write: operation not permitted` — the firewall DROP observed from a second, out-of-band actor (§11.4.13 sink-side-class corroboration). |
| Full proxy-data-plane-through-gluetun e2e | PENDING | — | The full dynamic proxy stack data-plane routed THROUGH gluetun (real proxy traffic → tunnel → Mullvad exit) is not yet captured. Tracked in the operator-blocked/pending surface above. NOT a claim the capability is broken — a not-yet-exercised data-plane step (§11.4.6). |

## Honest boundary (§11.4.6)

This document reconciles what the Mullvad WireGuard egress feature has PROVEN this session with captured live evidence: a **persistent one-device Mullvad identity** (device "groovy rabbit"), **live egress** (`mullvad_exit_ip=true`, deterministic 3/3), and the **kill-switch / fail-closed** guarantee (tunnel-DOWN ⇒ all egress BLOCKED, deterministic 3/3, corroborated by gluetun's own healthcheck hitting the firewall). Two follow-ups remain honest, not hidden: the full **proxy-data-plane-through-gluetun end-to-end** path is OWED/PENDING (the tunnel + kill-switch are proven; wiring the whole proxy stack through them is the next data-plane step), and **live DNS** is OPERATOR-BLOCKED (operator-deferred on 2026-07-02, Let's Encrypt Phase 4/6 — a downstream operator decision, not an egress-feature defect). All credentials are in the gitignored `.env` only and never appear in this document or its evidence (§11.4.10). None of this substitutes for the §11.4.40 full-suite retest before a release tag.
