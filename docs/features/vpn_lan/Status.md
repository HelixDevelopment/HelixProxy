# helix_proxy — VPN-LAN Service Access Feature Status

**Revision:** 4
**Last modified:** 2026-07-02T05:52:00Z
**Status:** Feature COMPLETE (Phases 0-12 on `main`). Anti-bluff bridge scaffold + preflight doctor + operator guide + Challenge runner + **Phase-1 SSRF carve-out teeth** + **Phase-12 bidirectional ingress-allowlist teeth** + the **§11.4.135 autonomous battery** (stress+chaos, benchmark+memory, concurrency+load, DNS-rebinding gap) are PROVEN NOW (autonomously, no live VPN, no podman) and wired GREEN into the standing suite. **NEW (§J, §11.4.52 hermetic promotions):** the Chromecast-eureka, FTP-passive, WebDAV-PROPFIND, and email (SMTPS/IMAPS/POP3S) protocol legs now ALSO run **AUTONOMOUSLY** — their client-side protocol logic proven over a real rootless kernel-WireGuard tunnel between two unprivileged netns against a pure-stdlib peer (zero installs, no operator/podman/VPN), each with golden-bad teeth + a not-stale self-fetch + 3/3 determinism + independent GO; **all four are wired into the standing suite as §11.4.135 regression guards (`test_vpn_lan_hermetic` in `tests/run-tests.sh`, SKIP-aware)**; the **live** Mullvad round-trip stays operator-gated (these COMPLEMENT, never replace, §11.4.6). Every live protocol round-trip (SMB/NFS/FTP/SFTP/WebDAV/email/Cast/DIAL/ADB) is AUTHORED with real byte-integrity / sink-side evidence gating **plus a Phase-12 both-way reverse-leg assertion** (NFS NLM/NSM callback, FTP active, Cast callback, adb reverse, email N/A §11.4.6) and honestly SKIPs while the svord bridge is down (§11.4.3) — never a fake PASS; Miracast is Won't-fix structurally-impossible (§11.4.112). Operator-gated: the svord bridge (secrets + Mullvad + root/sudo) → unlocks all live round-trips BOTH directions; the multicast-reflector remote deployment; any real-device flash. **⚠️ Data-plane env-block (operator-actionable, NOT a code defect):** host rootless-podman `aardvark-dns` cannot bind netavark `:53` → `:53128`/`:51080` don't serve + `./start` fails; the standing suite honestly reports this (3 env FAILs) rather than hanging.
**Authority:** Inherits `constitution/Constitution.md` per §11.4.35. This is the §11.4.153 per-feature Status set (companion §11.4.56 two-audience [`Status_Summary.md`](Status_Summary.md)) for the VPN-LAN service-access feature workstream (`feature/vpn-aware-dynamic-routing`, §11.4.167). Every row is reconciled against real committed code + tests + design docs; no metadata-only PASS and no fabricated video-confirmation (§11.4 / §11.4.1 / §11.4.6).
**Companion / cross-refs:** integration status [`../../design/vpn_lan_access/Status.md`](../../design/vpn_lan_access/Status.md) · plan [`../../design/vpn_lan_access/PLAN.md`](../../design/vpn_lan_access/PLAN.md) · Miracast verdict [`../../design/vpn_lan_access/miracast_verdict.md`](../../design/vpn_lan_access/miracast_verdict.md) · reflector design [`../../design/vpn_lan_access/reflector_design.md`](../../design/vpn_lan_access/reflector_design.md) · operator guide [`../../guides/vpn_lan_bridge_setup.md`](../../guides/vpn_lan_bridge_setup.md).
**Repo anchor:** branch `main` (all work merged to + performed on main per operator directive). Landing commits: bridge scaffold `d781002` + standing-suite wiring `5c28f56`; Phase 2/3 file protocols `182e80a`; Phase 4 email `2f31460`; Phase 5 reflector `d0b42df`; Phase 6/7 Cast+ADB `65043ce`; Phase 8 Miracast `12faf12`; Phase 9 guide `a5e5616`; Phase 11 Challenge runner + HelixQA bank `89f73b7`; Phase 1 SSRF teeth `1d87b3a`; Phase 10 containerization `1911d5e`; Phase 12 bidirectional `2ed0fed` + both-way protocol assertions `cc8b620`; stress+chaos `97d9733`; bench+memory + dns-rebinding `4e2810c`; concurrency+load `7e73b08`; §11.4.135 suite guards `6d8893f`; **hermetic H0 substrate + H2 Chromecast-eureka promotion `18a21bd`; H2.ftp + H2.webdav promotions `3b98d02`** (§J, §11.4.52).

---

## Operator-blocked / operator-gated items (read first — §11.4.45 O(1) surface)

Every autonomous claim below is either PROVEN NOW against a committed file/evidence artefact, or an honest SKIP that flips to a live PASS the moment the operator supplies the input named here. No live round-trip is marked PASS without captured evidence (§11.4.6 / §11.4.69). **Note (§J):** the FTP-passive, WebDAV-PROPFIND, and Chromecast-eureka legs' *client-side protocol logic* is ALREADY proven AUTONOMOUSLY over a hermetic WireGuard tunnel (no operator/VPN needed) — only the *live Mullvad-topology* round-trip below stays operator-gated.

| Item | Why blocked / gated | Unblock condition |
|---|---|---|
| Live protocol round-trips (SMB / NFS / FTP / FTPS / SFTP / WebDAV / IMAP / SMTP-submission / POP3S / Cast / DIAL / ADB) | Need the live svord bridge — secrets + Mullvad WireGuard + root/sudo on the bridge side (§11.4.21 / §11.4.10). The scaffold proves the honest-SKIP path + the anti-bluff gate NOW; the live byte/sha256/mailbox/serial evidence exists only when the bridge is genuinely up. Bridge-down ⇒ honest SKIP (`network_unreachable_external`), never a fake PASS (§11.4.3 / §11.4.69). | Operator copies `.env.example`→`.env`, points the 6-var contract at `svord_toolkit`, brings the VPN up; each test then emits real captured round-trip evidence under `qa-results/vpn_lan/phase<N>/<ts>/`. |
| Multicast discovery reflector — remote deployment | Deploying an Avahi mDNS / SSDP-relay reflector changes a remote host (§11.4.122) — requires an interactive keep/deploy decision. The reflector *design* + the client-side enumeration *test* are committed and autonomous; only the remote deploy is gated. | Operator authorizes the remote-side reflector deployment (Phase 10 boots it on-demand via the containers submodule, §11.4.76). |
| ADB flash over VPN (`usbip` USB-over-IP) | Flashing a real device is high-blast-radius (§11.4.133 target-hardware-safety) and USB-level, not network-routable (fastboot is USB-bound — recon 4 FACT). The test NEVER flashes; it records the honest boundary and SKIPs `operator_attended`. | Operator authorizes the specific device flash via a remote host with the device attached over `usbip`. |
| HelixQA `vpn_lan.yaml` autonomous session | The `helixqa` binary cannot be built in this checkout — its `go.mod` `replace`s 6 un-vendored own-org sibling modules (§11.4.3). The bank is authored; no autonomous HelixQA session has been run and none is claimed. The runnable proof NOW is the Challenge runner. | Vendor the 6 sibling modules under `submodules/`, then drive the bank via an autonomous HelixQA session. |

---

## Feature status table (§11.4.153)

**Legend.** *Wiring* = is the feature genuinely reachable/usable by an end user of the deployed helix_proxy through the env-var bridge (§11.4.108)? *Validation* ∈ {PASS, FAIL, SKIP, PENDING_FORENSICS, OPERATOR-BLOCKED}. A **PASS** cites a committed captured artefact or a self-proving standing-suite gate. **SKIP** = honest topology/precondition skip (bridge down / reflector not deployed / server absent — §11.4.3), never folded into a PASS. *Video-confirmation* is **N/A** for every row: helix_proxy VPN-LAN is a headless network feature with no rendered UI — its §11.4.107/§11.4.69 proof is captured exit codes + byte-integrity sha256 + sink-side protocol responses (SMB/NFS/mailbox/eureka JSON/device serial), not a rendered video (§11.4.153(3) autonomous-infeasible ⇒ honest non-video evidence). Live-path rows additionally carry no autonomous video because the live round-trip needs the operator-gated svord bridge.

### A. Bridge foundation & anti-bluff scaffold — PROVEN NOW (autonomous, no live VPN)

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| env-var bridge contract | 6-var decoupled bridge contract (`HELIX_SVORD_DIR` / `_CONNECT` / `_DISCONNECT` / `_HEALTH` / `_SUBNET` / `_HOST`); `bridge_load` / `bridge_up` / `bridge_require` primitives (rc 2 = OPERATOR-BLOCKED, rc 3 = misconfigured) | foundation / decoupling (§11.4.28) | `.env.example` (tracked, no secrets); `tests/lib/svord_bridge.sh` | YES — every VPN-LAN test sources it; `.env`→bridge is the single, documented, gitignored wiring point (§11.4.10 / §11.4.30) | Operator sources `.env`; the 6 vars resolve or `bridge_load` lists the unset names; no svord path is hardcoded | unit (contract resolve + set-u safety) + wired into standing suite `test_vpn_lan_bridge` + §11.4.115 teeth (healthy stub ⇒ UP) + §1.1 mutation surface | **PASS** (`d781002`; standing suite GREEN `qa-results/suite/run_20260701T160156Z.log` 71/64/7/0) | N/A — headless; proof is the contract-resolution + suite verdict, not video |
| svord-doctor preflight | Deterministic 3-verdict preflight — `BRIDGE: UP` (exit 0) / `SKIP:network_unreachable_external` (exit 2) / `MISCONFIGURED:<reason>` (exit 3): env resolvable? hooks executable? `HELIX_BRIDGE_HEALTH` exit 0? remote host reachable? Never fakes UP | preflight / anti-bluff gate | `scripts/svord_doctor.sh`; companion `docs/scripts/svord_doctor.md` (§11.4.18) | YES — the mandatory first step every protocol test + the Challenge runner run; a DOWN/MISCONFIGURED verdict forces honest downstream SKIP | Operator runs `sh scripts/svord_doctor.sh`; bridge-down NOW ⇒ `MISCONFIGURED:env_unset` / `SKIP` (honest), never UP | standing suite `test_vpn_lan_bridge` (3-verdict discrimination) + §11.4.115 teeth (local UP-stub ⇒ `BRIDGE: UP`, proving the SKIP is conditional not rubber-stamp) + `run-tests.sh:821-872` | **PASS** (`d781002`/`5c28f56`; teeth GREEN in standing suite) | N/A — headless; proof is the `BRIDGE:` verdict line + teeth, not video |

### B. File shares — AUTHORED, live round-trip operator-gated

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| SMB / CIFS / NMB | SMB/CIFS write→read-back→sha256 round-trip over the L3-routed VPN share (`smbclient`, `mount.cifs` fallback); NMB name-resolution by 10.x unicast (multicast NMB not routed) | file-share / functional | `tests/vpn_lan/smb_nfs_roundtrip.sh` (SMB slice) | Gated — reachable once the bridge is up + `HELIX_VPN_SMB_UNC` set; routes over L3, NOT SOCKS5 | Bridge-down NOW ⇒ honest SKIP + exit 0, zero `^PASS:` lines; bridge-up ⇒ real sha256-matched round-trip evidence | test authored + `ab_pass_with_evidence` (empty artefact ⇒ refused) + honest-SKIP gate + Challenge case `VLA-SMB` + HelixQA `VLA-SMB` | **SKIP** (operator-gated live path; `182e80a`) | N/A — headless; live path operator-gated (no autonomous video) |
| NFS | NFS write→read-back→sha256 round-trip over the VPN export (pre-mounted `HELIX_VPN_NFS_MOUNTED`, or root temp-mount `HELIX_VPN_NFS_EXPORT`); temp mount unmounted on every exit (§11.4.14) | file-share / functional | `tests/vpn_lan/smb_nfs_roundtrip.sh` (NFS slice) | Gated — reachable once bridge up + export configured; routes over L3 | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ sha256-matched round-trip; sha256 mismatch ⇒ FAIL (teeth) | test authored + evidence-gated PASS + cleanup trap + Challenge case `VLA-NFS` + HelixQA `VLA-NFS` | **SKIP** (operator-gated live path; `182e80a`) | N/A — headless; live path operator-gated |

### C. File transfer — AUTHORED, live round-trip operator-gated

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| FTP / FTPS | FTP passive directory-list + fetch over routed :21 + pinned PASV range (PASV must advertise the 10.x addr); FTPS explicit (`AUTH TLS`) / implicit (:990) | file-transfer / functional | `tests/vpn_lan/ftp_sftp_webdav.sh` (FTP slice) | Gated — reachable once bridge up + `HELIX_VPN_FTP_URL` set | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ captured non-empty listing + byte fetch evidence | test authored + evidence-gated PASS + Challenge case `VLA-FTP` + HelixQA `VLA-FTP` | **SKIP** (operator-gated live path; `182e80a`) | N/A — headless; live path operator-gated |
| SFTP | SFTP single-connection byte round-trip over routed :22 (key-based, `BatchMode` — fully autonomous, §11.4.98); sha256 match required | file-transfer / functional | `tests/vpn_lan/ftp_sftp_webdav.sh` (SFTP slice) | Gated — reachable once bridge up + `HELIX_VPN_SFTP_HOST` set | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ sha256-matched round-trip; sha256 mismatch ⇒ FAIL (teeth) | test authored + evidence-gated PASS + Challenge case `VLA-SFTP` + HelixQA `VLA-SFTP` | **SKIP** (operator-gated live path; `182e80a`) | N/A — headless; live path operator-gated |
| WebDAV (via existing Squid) | WebDAV PROPFIND through the EXISTING Squid (no new component) — expect `207 Multi-Status` + non-empty XML body; a real non-207 HTTP response ⇒ FAIL (fail-closed §11.4.68) | file-transfer / functional | `tests/vpn_lan/ftp_sftp_webdav.sh` (WebDAV slice) | Gated — reachable once bridge up + `HELIX_VPN_WEBDAV_URL` set; traverses `HELIX_SQUID_PROXY` (default `:53128`) | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ captured 207 + XML; wrong-answer (non-207) ⇒ FAIL not SKIP | test authored + wrong-answer-FAIL teeth + Challenge case `VLA-WEBDAV` + HelixQA `VLA-WEBDAV` | **SKIP** (operator-gated live path; `182e80a`) | N/A — headless; live path operator-gated |

### D. Email — AUTHORED, live round-trip operator-gated

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| IMAP / IMAPS | IMAPS `LOGIN` + `LIST "" "*"` over implicit-TLS :993 (RFC 8314) — assert a real mailbox `* LIST` listing returns; `a1 NO/BAD` ⇒ FAIL | email / functional | `tests/vpn_lan/email_roundtrip.sh` (T4.1) | Gated — reachable once bridge up + `HELIX_MAIL_USER/PASS` set; creds via in-process `printf` stdin, never argv/logged (§11.4.10) | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ captured mailbox LIST body | test authored + evidence-gated PASS + Challenge case `VLA-EMAIL` + HelixQA `VLA-EMAIL` | **SKIP** (operator-gated live path; `2f31460`) | N/A — headless; live path operator-gated |
| SMTP submission | Authenticated submission send over :465 implicit / :587 STARTTLS — a self-addressed round-trip probe token must be 2xx/250-accepted | email / functional | `tests/vpn_lan/email_roundtrip.sh` (T4.2) | Gated — reachable once bridge up + creds set | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ captured 250 acceptance; non-2xx end-of-DATA ⇒ FAIL | test authored + evidence-gated PASS + round-trip token links to POP3S retrieve + Challenge/HelixQA `VLA-EMAIL` | **SKIP** (operator-gated live path; `2f31460`) | N/A — headless; live path operator-gated |
| POP3S | POP3S retrieve over implicit-TLS :995 — retrieve the just-sent T4.2 message and verify its unique token (a true send→retrieve round-trip); token not returned within retries ⇒ FAIL | email / functional | `tests/vpn_lan/email_roundtrip.sh` (T4.3) | Gated — reachable once bridge up + creds + a successful T4.2 send | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ captured retrieved message body carrying the sent token | test authored + evidence-gated PASS + retry window + Challenge/HelixQA `VLA-EMAIL` | **SKIP** (operator-gated live path; `2f31460`) | N/A — headless; live path operator-gated |
| Open-relay guard (mandatory negative) | §4.3 MANDATORY negative test — an UNAUTHENTICATED relay to an EXTERNAL domain MUST be REFUSED: captured 4xx/5xx of the external RCPT ⇒ PASS; a captured 2xx acceptance ⇒ decisive FAIL (helix_proxy is NEVER an open relay / spam conduit); incomplete probe ⇒ honest SKIP (never fake-PASS the guard) | email / security | `tests/vpn_lan/email_roundtrip.sh` (T4.4) | Gated — reachable once bridge up + mail server present | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ captured refusal of the external-domain RCPT | test authored + external-RCPT-accepted-⇒-FAIL teeth + no-DATA/RSET safety + Challenge/HelixQA `VLA-EMAIL` (category `security`) | **SKIP** (operator-gated live path; `2f31460`) | N/A — headless; live path operator-gated |

### E. Discovery reflector — AUTHORED design + test, remote deploy operator-gated

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| mDNS / DNS-SD / SSDP / WS-Discovery reflector | Remote-side multicast discovery reflector (Avahi `enable-reflector` for mDNS 5353 / DNS-SD; SSDP relay for 1900; WS-Disc 3702) — multicast is subnet-local and NOT forwarded across the L3 VPN, so a remote-side reflector is required. Client test: `avahi-browse -rpt` surfaces a resolved (`^=`) remote service (SCORED); SSDP M-SEARCH count is supplementary/non-scored | discovery / functional | design `docs/design/vpn_lan_access/reflector_design.md`; test `tests/vpn_lan/discovery_reflect.sh` | Gated — enumeration reachable once bridge up + reflector deployed (`HELIX_VPN_REFLECTOR`); remote deploy is operator-gated (§11.4.122) | Bridge-down / no-reflector NOW ⇒ honest SKIP (`feature_disabled_by_config`); reflector up ⇒ captured resolved DNS-SD record | design doc + client enumeration test (evidence-gated PASS, empty output ⇒ SKIP not PASS) + honest-SKIP gate + §1.1 surface | **SKIP** (reflector deploy operator-gated; `d0b42df`) | N/A — headless; live path operator-gated |

### F. Casting (Google Cast / DIAL) — AUTHORED, live round-trip operator-gated

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| Chromecast eureka_info control | Routed `GET http://<ip>:8008/setup/eureka_info` over the L3 VPN — a real JSON `name` field is the device-identity evidence; HTTP 200 without a JSON `name`, or a non-200 answer ⇒ FAIL (fail-closed §11.4.68) | casting / functional | `tests/vpn_lan/chromecast_dial.sh` (T6.2) | Gated — reachable once bridge up + `HELIX_VPN_CAST_IP` set; control routes as unicast TCP | Bridge-down NOW ⇒ honest SKIP; bridge-up ⇒ captured eureka_info JSON `name` + CASTV2 :8009 TLS-reachability note | test authored + JSON-name / non-200-FAIL teeth + Challenge case `VLA-CAST` + HelixQA `VLA-CAST` | **SKIP** (operator-gated live path; `65043ce`) | N/A — headless; live path operator-gated |
| CASTV2 liveness (§11.4.107) | Cast status *transition* observed across two reads (advancing state, NOT a single frozen frame) via an operator-supplied status command; two DISTINCT reads ⇒ transition PASS; identical reads (device idle) ⇒ honest `operator_attended` SKIP (a transition needs media playing), never a fake PASS | casting / functional (liveness) | `tests/vpn_lan/chromecast_dial.sh` (T6.3) | Gated — reachable once bridge up + `HELIX_VPN_CAST_STATUS_CMD` set | Bridge-down / no-status-tool NOW ⇒ honest SKIP; bridge-up + media ⇒ captured status-transition cksum delta | test authored + §11.4.107 transition-not-frame oracle + Challenge/HelixQA `VLA-CAST` | **SKIP** (operator-gated live path; `65043ce`) | N/A — headless; live path operator-gated |
| DIAL discovery + control | DIAL discovery via the Phase-5 reflector (SSDP `M-SEARCH`) + `_googlecast._tcp` mDNS; HTTP control routes over the VPN as ordinary unicast TCP (the routable "cast to a remote display" alternative to Miracast) | casting / functional | `tests/vpn_lan/chromecast_dial.sh` (T6.1 discovery) + `reflector_design.md` | Gated — discovery depends on the reflector (E); control routes once bridge up | Bridge-down / no-reflector NOW ⇒ honest SKIP; reflector up ⇒ reflected `_googlecast._tcp` enumeration | discovery-via-reflector test + evidence-gated PASS + Challenge/HelixQA `VLA-CAST` | **SKIP** (operator-gated live path; `65043ce`/`d0b42df`) | N/A — headless; live path operator-gated |

### G. Device bridge (ADB over VPN) — AUTHORED; flash operator-blocked

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| ADB connect / shell / getprop | `adb connect <host>:5555` over the routed VPN (no proxy hop — recon 4) + central adb-server model; assert OUR serial reaches `device` state then `adb -s <serial> shell getprop ro.product.model` returns real content; reachable-but-unusable state (offline/unauthorized) ⇒ FAIL. Safety: touches ONLY the env-configured serial — never `kill-server`, never a blanket disconnect, never any other/operator/lava-* device (§11.4.174); `adb disconnect <our-serial>` in the cleanup trap | device-bridge / functional | `tests/vpn_lan/adb_over_vpn.sh` (T7.1/T7.3) | Gated — reachable once bridge up + `HELIX_VPN_ADB_HOST` set | Bridge-down NOW ⇒ honest SKIP (adb never invoked, no device touched); bridge-up ⇒ captured serial + getprop model content | test authored + own-serial-only §11.4.174 safety + not-`device`-state-⇒-FAIL teeth + Challenge case `VLA-ADB` + HelixQA `VLA-ADB` | **SKIP** (operator-gated live path; `65043ce`) | N/A — headless; live path operator-gated |
| ADB flash (usbip USB-over-IP) | Honest boundary (§11.4.6): `fastboot` is USB-level and network fastboot is USB-bound (recon 4 FACT); the routable path is `usbip` from a remote host with the device attached. The test NEVER flashes and NEVER invokes fastboot/usbip — it records the boundary and SKIPs `operator_attended`; any real-device flash is operator-gated (§11.4.133 + §11.4.122) | device-bridge / operator-safety | `tests/vpn_lan/adb_over_vpn.sh` (T7.4) | Gated — real-device flash requires explicit operator authorization | NOW ⇒ documented boundary evidence + honest `operator_attended` SKIP; no flash ever attempted autonomously | test authored + boundary-record + never-flashes safety + Challenge/HelixQA `VLA-ADB` note | **OPERATOR-BLOCKED** (real-device flash §11.4.133 + §11.4.122; `65043ce`) | N/A — headless; operator-gated |

### H. Miracast — Won't-fix, structurally-impossible

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| Miracast over L3 VPN | Won't-fix — structurally-impossible (§11.4.112): Miracast rides Wi-Fi Direct / 802.11 P2P at Layer 2 with no routable IP hop for an L3 VPN to carry; cited Wi-Fi-Alliance + Wi-Fi-Direct evidence. The routable "cast to a remote display" capability is delivered by Google Cast / DIAL (§F). NO fake traversal test exists or will be authored (a green Miracast-over-VPN result would be fabricated) | honest-boundary / structural verdict | `docs/design/vpn_lan_access/miracast_verdict.md` (cited spec text IS the artifact) | N/A — out of scope by physics; positive capability lives in §F (Cast/DIAL) | The verdict doc is the deliverable; reopen only on NEW evidence the platform constraint changed (§11.4.34 / §11.4.7) | verdict doc with cited authoritative sources (§11.4.99 latest-source, access 2026-07-01); no fake test (correct posture §11.4.112) | **PASS (Won't-fix, §11.4.112)** (`12faf12`) | N/A — a structural verdict, not a runnable/renderable capability |

### I. Documentation & verification infrastructure

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| Operator bridge-setup guide | End-user manual — overview, one-line routing map, the 6-var contract, setup steps, per-protocol matrix, security notes, FAQ; HTML+PDF exports §11.4.65, §11.4.168 visually validated (no raw diagram source leaking) | docs / operator | `docs/guides/vpn_lan_bridge_setup.md` | YES — the operator's out-of-the-box entry point to enable the feature | Operator reads the guide, copies `.env.example`→`.env`, points the bridge at `svord_toolkit`, runs the doctor | doc authored + §11.4.44 revision header + §11.4.65 HTML+PDF exports leak-clean | **PASS** (`a5e5616`) | N/A — document; §11.4.168 exported-doc visual validation is its proof, not video |
| VPN-LAN Challenge runner | Runnable anti-bluff harness — runs `svord_doctor.sh` preflight then each `tests/vpn_lan/*.sh`, maps exit codes (doctor 0=UP/2=SKIP/3=MISCONFIGURED; tests 0=no-FAIL/1=FAIL), tallies PASS/FAIL/SKIP with per-item logs under host-safety caps (`GOMAXPROCS=2 nice ionice`, §12.6). Bridge-down ⇒ every item honest SKIP, exit 0 (never a fake PASS); a not-yet-present test ⇒ SKIP, never FAIL | verification / Challenge (§11.4.27) | `challenges/scripts/run_vpn_lan_challenges.sh` | YES — an operator can run it NOW; it wraps the committed doctor + protocol tests + §1.1-hardened tally | Operator runs `bash challenges/scripts/run_vpn_lan_challenges.sh`; NOW ⇒ honest all-SKIP tally (bridge down), exit 0 | runner authored + exit-code→verdict mapping wired to the committed tests + `sh -n`/`bash -n` clean (§11.4.67); live-bridge PASS tally operator-gated | **PASS** (runnable harness + honest mapping proven; live tally operator-gated; `89f73b7`) | N/A — headless; proof is the tally + per-item logs, not video |
| HelixQA `vpn_lan.yaml` bank | HelixQA test bank — one case per protocol (`VLA-BRIDGE-PREFLIGHT` / `VLA-SMB` / `VLA-NFS` / `VLA-FTP` / `VLA-SFTP` / `VLA-WEBDAV` / `VLA-EMAIL` / `VLA-CAST` / `VLA-ADB`) dispatching `ActionTypeShell` to the real protocol test so the case verdict IS the real test verdict; documents the exit-code→verdict mapping | verification / HelixQA (§11.4.27) | `tools/helixqa/banks/vpn_lan.yaml` | Gated — bank authored; the `helixqa` binary is un-buildable here (6 un-vendored own-org siblings) | NOW ⇒ authored-but-run-blocked; the runnable proof is the Challenge runner; no autonomous HelixQA session claimed | bank authored (schema-valid `shell:` actions) + honest run-blocked SKIP (§11.4.3); Challenge runner is the runnable surrogate | **SKIP** (authored-but-run-blocked §11.4.3; `89f73b7`) | N/A — headless; would be the bank's `result.json`, not video |

### J. Hermetic-tunnel autonomous protocol promotions (§11.4.52) — PROVEN NOW (autonomous, no live VPN, no operator)

**What these add.** Four protocol legs that were previously *only* operator-gated SKIP now ALSO have an **autonomous PASS path** (all four wired into the standing suite as §11.4.135 regression guards via `test_vpn_lan_hermetic`, `71e0bac`): their client-side protocol logic is proven over a **real kernel-WireGuard tunnel** between two unprivileged network namespaces (`unshare -Urnm`, veth underlay `10.9.0.x` + `wg0` overlay `10.10.0.x`), with a **pure-python-stdlib peer** serving the protocol — **zero host installs**, no podman, no live Mullvad, no operator. Each harness drives the **UNMODIFIED** protocol test inside netns A (its `bridge_require` gate flips SKIP→UP), carries a **golden-bad mutation** that MUST make the real assertion FAIL (§11.4.107(10)/§11.4.68 teeth), and a **self-fetch not-stale cross-check** (§11.4.107) — all **3/3 deterministic** (§11.4.50) and **independently reviewed to GO** (§11.4.142). **Honest boundary (§11.4.6):** these prove the *client-side protocol logic* over an encrypted tunnel against a *controlled* peer — they do NOT prove the real live server on the Mullvad topology (that stays the operator-gated live round-trip above), and the peer bodies are controlled fixtures. They COMPLEMENT, never replace, the live rows.

| Component | Feature | Category | Implementation | Wiring (reachable?) | Real-use | Tests-coverage (four-layer §11.4.4(b)) | Validation | Video-confirmation |
|---|---|---|---|---|---|---|---|---|
| H0 hermetic WG substrate | Rootless two-netns kernel-WireGuard round-trip (veth underlay + real `wg0` overlay); the reusable substrate every H2.x promotion builds on | test-harness / foundation | `tests/vpn_lan/hermetic_netns_poc.sh` (veth L3) + `hermetic_wg_roundtrip.sh` (full WG) | YES — runnable NOW with zero installs; SKIPs honestly if `wireguard` module / `unshare -Urnm` unavailable (§11.4.3) | Anyone runs the two scripts NOW → sha256-verified L3 + tunnel round-trip, self-bounded (§12) | PoC + full-WG round-trip, golden-bad (bad WG key ⇒ no traversal), 3/3 deterministic; companion docs (§11.4.18) | **PASS** (`18a21bd`; `docs/scripts/hermetic_netns_poc.md` / `hermetic_wg_roundtrip.md`) | N/A — headless; proof is the sha256-matched round-trip + teeth |
| Chromecast eureka_info — hermetic promotion | The UNMODIFIED `chromecast_dial.sh` eureka control leg run AUTONOMOUSLY over the hermetic tunnel against a stdlib `eureka_info` JSON peer on `10.10.0.2:8008`; golden-bad = a name-less body ⇒ the real test FAILs fail-closed | casting / functional (autonomous) | `tests/vpn_lan/hermetic_bridge_run.sh` | YES NOW — no operator/podman/VPN; self-fetch asserts this run's fresh `name` nonce over the tunnel (§11.4.107 not-stale) | Run `tests/vpn_lan/hermetic_bridge_run.sh` → real eureka `name` PASS over `wg0`; `H2_MUT=badeureka` ⇒ teeth fire | promoted-test drive + golden-bad teeth + self-fetch cross-check + coupling guard, 3/3 deterministic; companion `docs/scripts/hermetic_bridge_run.md` | **PASS** (autonomous protocol logic; live Mullvad round-trip still §F SKIP; `18a21bd`) | N/A — headless; proof is the promoted-test PASS + teeth + evidence dir |
| FTP passive — hermetic promotion | The UNMODIFIED `ftp_sftp_webdav.sh` FTP leg run AUTONOMOUSLY over the hermetic tunnel against an embedded ~85-line stdlib FTP server on `10.10.0.2:2121` (PASV advertises `10.10.0.2`, EPSV carries none — both traverse `wg0`); harness ADDITIONALLY content-verifies a byte RETR of a fresh nonce (§11.4.107(9)); golden-bad = empty listing ⇒ the real test SKIPs (no PASS) | file-transfer / functional (autonomous) | `tests/vpn_lan/hermetic_ftp_run.sh` | YES NOW — no operator/podman/VPN; self-list + content-verified self-RETR over the tunnel | Run `tests/vpn_lan/hermetic_ftp_run.sh` → real passive-listing PASS + byte round-trip; `FT_MUT=empty` ⇒ teeth fire | promoted-test drive + golden-bad teeth + self-RETR ground-truth check + ambient-env determinism scrub, 3/3 deterministic; companion `docs/scripts/hermetic_ftp_run.md` (Rev 2) | **PASS** (autonomous protocol logic; live Mullvad round-trip still §C SKIP; `3b98d02`) | N/A — headless; proof is the promoted-test PASS + self-RETR + teeth |
| WebDAV PROPFIND — hermetic promotion | The UNMODIFIED `ftp_sftp_webdav.sh` WebDAV leg run AUTONOMOUSLY over the hermetic tunnel: a stdlib `207 Multi-Status` origin on `10.10.0.2:8080` reached THROUGH a stdlib forward proxy on `127.0.0.1:3128` (RFC 7230 §5.3.2→§5.3.1); golden-bad = origin flips to `200` ⇒ the real test FAILs fail-closed (§11.4.68) — the mutation surface IS exactly the promoted test's only PASS gate | file-transfer / functional (autonomous) | `tests/vpn_lan/hermetic_webdav_run.sh` | YES NOW — no operator/Squid/VPN; self-PROPFIND asserts 207 + this run's fresh nonce (§11.4.107 not-stale) | Run `tests/vpn_lan/hermetic_webdav_run.sh` → real 207 PASS through the proxy; `WEBDAV_MUT=bad207` ⇒ teeth fire | promoted-test drive + golden-bad teeth + self-PROPFIND cross-check + ambient-env determinism scrub, 3/3 deterministic; companion `docs/scripts/hermetic_webdav_run.md` (Rev 1) | **PASS** (autonomous protocol logic; live Mullvad round-trip still §C SKIP; `3b98d02`) | N/A — headless; proof is the promoted-test 207 PASS + self-fetch + teeth |
| Email round-trip (SMTPS/IMAPS/POP3S) — hermetic promotion | The UNMODIFIED `email_roundtrip.sh` run AUTONOMOUSLY over the hermetic tunnel against a pure-stdlib **implicit-TLS** mail peer (SMTPS 465 / IMAPS 993 / POP3S 995, shared in-memory mailbox) on `10.10.0.2`; SMTP DATA embeds a per-run nonce that POP3S RETR MUST return (round-trip oracle §11.4.107); **two** golden-bad teeth — `MAIL_MUT=openrelay` ⇒ real `FAIL: open_relay_refused` (external RCPT accepted), `MAIL_MUT=droptoken` ⇒ real `FAIL: pop3s_retrieve_roundtrip` (token not retrieved) — each fails ONLY its target leg | email / functional (autonomous) | `tests/vpn_lan/hermetic_email_run.sh` | YES NOW — no operator/podman/VPN; peer binds WG-only `10.10.0.2` so a successful login/submit/retrieve = tunnel traversal (§11.4.111); TLS `close_notify` clean-close (unwrap-before-close, incl. IMAP bare-QUIT) prevents silent-SKIP masking (§11.4.6) | Run `tests/vpn_lan/hermetic_email_run.sh` → all 4 scored legs PASS over `wg0`; each `MAIL_MUT` ⇒ its target leg FAILs | promoted-test drive + two targeted golden-bad teeth + self-fetch not-stale nonce + gate requires all four `^PASS:` tokens + §11.4.10 no-cred-leak scan, 3/3 deterministic; companion `docs/scripts/hermetic_email_run.md` (Rev 1) | **PASS** (autonomous protocol logic; live Mullvad mail round-trip stays operator-gated §11.4.3; `3b73f02`, independent §11.4.142 GO) | N/A — headless; proof is the 4-leg PASS + both teeth + no-cred-leak scan |

**Not hermetic-promotable — honest verdict (§11.4.6 / §11.4.3).** The clean zero-install promotion set is now **COMPLETE** (Cast-eureka + FTP + WebDAV + email above). The remaining protocol legs are genuinely operator-gated: SFTP needs an SSH server (`sshd` absent on host + no stdlib SSH server; paramiko/asyncssh are pip → §11.4.122); SMB/NFS need samba/nfsd (absent + root); the `discovery_reflect.sh` scored mDNS leg hard-requires the `avahi-browse` *client* (absent) so a stdlib mDNS responder wouldn't help the UNMODIFIED test; ADB/container need a device/podman. **Follow-up hardening (separate reviewed batch):** add an explicit wrong-destination negative (underlay/wg-down MUST-fail) so §11.4.111 reachability-as-proof is self-evidencing, + a `/usr/sbin/wg` preflight across the sibling harnesses (the email harness already handles it). Deep-research: [`../../research/hermetic_protocol_promotion_20260702/FINDINGS.md`](../../research/hermetic_protocol_promotion_20260702/FINDINGS.md) (Rev 2, §6 mail).

---

## §11.4.169 test-type coverage note (honest boundary)

The autonomously-provable layers today are: **unit / config** (bridge-contract resolution + set-u safety), the **standing-suite gate** (`test_vpn_lan_bridge` — 3-verdict discrimination + §11.4.115 UP-stub teeth, GREEN), the **Challenge runner** (runnable NOW, honest bridge-down all-SKIP tally), and the **structural verdict** (Miracast, cited). The **integration / e2e / full-automation / security / stress+chaos** live layers per protocol are AUTHORED with evidence-gating + wrong-answer-FAIL teeth but **honestly SKIP** until the operator connects the svord bridge (§11.4.3) — they are never counted as PASS on a down bridge. HelixQA autonomous-session coverage is **run-blocked** (6 un-vendored siblings, §11.4.3), surrogated by the Challenge runner. No metadata-only / config-only / absence-of-error PASS is recorded anywhere in this ledger (§11.4 / §11.4.1 / §11.4.69).

## Honest boundary (§11.4.6)

What is PROVEN NOW, autonomously, with committed evidence: the decoupled env-var bridge contract, the svord-doctor 3-verdict preflight with §11.4.115 teeth in the GREEN standing suite, the honest-SKIP anti-bluff gate across every protocol test (bridge-down ⇒ SKIP + exit 0 + zero `^PASS:` lines — never a fake PASS), the wrong-answer-FAIL teeth (WebDAV non-207, SFTP/NFS/SMB sha-mismatch, open-relay external-RCPT-accepted, ADB not-`device`-state, Cast non-200/no-name), the runnable Challenge harness, the operator guide, and the Miracast structural-impossibility verdict. What is **operator-gated** and therefore SKIP/OPERATOR-BLOCKED until unblocked: every **live** protocol round-trip (real bytes, sha256, mailbox content, eureka JSON, device serials — needs the svord bridge up), the multicast-reflector remote deployment (§11.4.122), any real-device flash (§11.4.133), and the autonomous HelixQA session (6 un-vendored siblings). No live capability is marked PASS without captured evidence on a genuinely-up bridge, and no feature is "done" for its live capability until its runtime signature verifies on a live bridge (§11.4.108) and it crosses the §11.4.169 test-type matrix + independent review (§11.4.142). This ledger does not substitute for the §11.4.40 full-suite retest before any release tag.

**Feature tally:** 4 rows PASS-now foundation (bridge contract · svord-doctor preflight · operator guide · Challenge runner) · **4 rows PASS-now §J hermetic autonomous protocol promotions** (H0 WG substrate · Chromecast-eureka · FTP-passive · WebDAV-PROPFIND — client-side protocol logic proven over a hermetic tunnel, zero installs, golden-bad teeth, 3/3 deterministic, independent GO) · 16 rows operator-gated SKIP/OPERATOR-BLOCKED for their **live Mullvad** round-trip (all live protocol round-trips + reflector deploy + ADB flash + HelixQA run; note the FTP/WebDAV/Cast-eureka *protocol logic* is additionally PASS via §J) · 1 row Won't-fix (Miracast, structurally-impossible §11.4.112). Email hermetic promotion is under active de-risking (§J footer).
