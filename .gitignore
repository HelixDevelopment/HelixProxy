logs
logs/*
logs/*.*

# Environment files (contain secrets)
.env
.env.local
.env.*.local

# VPN credentials
vpn-auth.txt
*.ovpn
credentials.txt

# Cache directories
cache/
 Cache/

# Logs
logs/*.log
*.log

# Configuration with sensitive data
config/squid/passwords
config/squid/certs/
config/htpasswd

# Runtime files
*.pid
*.sock
*.tmp

# Doc-export scratch (pandoc/weasyprint TMPDIR redirect — §11.4.30/§11.4.77:
# regenerated on demand by the export pipeline, never versioned)
.tmp_export/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/

# Temporary files
tmp/
temp/

# Captured test/QA evidence — raw working corpus.
# Regenerated by running the test/challenge suite (§11.4.77 regen mechanism);
# curated release proof is committed under docs/qa/ (§11.4.83/§11.4.128/§11.4.30).
qa-results/

# Redundant research archive — the extracted tree under
# docs/research/mvp/proxy-vpn-extension/ is the tracked, reviewable artifact (§11.4.30).
docs/research/mvp/*.tar.gz

# Hermetic mTLS test material — self-signed test CA/server/client certs, keys, and
# the random Postgres test password generated by tests/observability/gen_test_mtls.sh
# for the control-API self-boot (task #56). KEY MATERIAL MUST NEVER BE TRACKED
# (§11.4.10/§11.4.30); regenerated on demand by the script (§11.4.77 regen mechanism).
tests/observability/.mtls/
