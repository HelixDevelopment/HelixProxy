# Squid Proxy Configuration
# Tracked source config — mounted read-only into proxy-squid (docker-compose.yml).
# This IS the source of truth (no runtime generator); edit here + `squid -k reconfigure`.

# Network Settings
http_port 0.0.0.0:53128

# Cache Directory Configuration
cache_dir aufs /var/spool/squid 51200 16 256
cache_mem 512 MB

# Object Size Limits
maximum_object_size 100 MB
minimum_object_size 1 KB
maximum_object_size_in_memory 4 MB

# Cache Replacement Policy
cache_replacement_policy lru
memory_replacement_policy lru

# Access Control Lists
acl localnet src 0.0.0.1-0.255.255.255
acl localnet src 10.0.0.0/8
acl localnet src 100.64.0.0/10
acl localnet src 127.0.0.0/8
acl localnet src 169.254.0.0/16
acl localnet src 172.16.0.0/12
acl localnet src 192.168.0.0/16
acl localnet src fc00::/7
acl localnet src fe80::/10

# SSL/TLS Ports
acl SSL_ports port 443
acl Safe_ports port 80
acl Safe_ports port 21
acl Safe_ports port 443
acl Safe_ports port 70
acl Safe_ports port 210
acl Safe_ports port 1025-65535
acl Safe_ports port 280
acl Safe_ports port 488
acl Safe_ports port 591
acl Safe_ports port 777

# CONNECT method ACL
acl CONNECT method CONNECT

# Access Rules
http_access deny !Safe_ports
http_access deny CONNECT !SSL_ports
http_access allow localhost manager
http_access deny manager
http_access allow localnet
http_access allow localhost
http_access deny all

# Header hygiene & info-leak hardening (§11.4.169 security)
# forwarded_for delete — do NOT append X-Forwarded-For (client LAN IP) to upstream
#   requests; leaking it through the VPN egress defeats the proxy's privacy purpose.
# via off — do NOT emit `Via: 1.1 <hostname> (squid/<ver>)`; it leaks the internal
#   hostname + Squid version (incl. on the branded error pages).
# httpd_suppress_version_string on — no Squid version on generated error pages.
# visible_hostname — stable neutral identity instead of the container id.
forwarded_for delete
via off
httpd_suppress_version_string on
visible_hostname helix-proxy

# Logging
access_log /var/log/squid/access.log squid
cache_log /var/log/squid/cache.log
cache_store_log /var/log/squid/store.log
logfile_rotate 5

# PID file
pid_filename /var/run/squid/squid.pid

# Coredump directory
coredump_dir /var/spool/squid

# Refresh Patterns
refresh_pattern ^ftp:           1440    20%     10080
refresh_pattern ^gopher:        1440    0%      1440
refresh_pattern -i (/cgi-bin/|\?) 0     0%      0
refresh_pattern .               0       20%     4320

# DNS Configuration
dns_nameservers 8.8.8.8 8.8.4.4
positive_dns_ttl 3600 seconds
negative_dns_ttl 30 seconds

# Performance Tuning
server_persistent_connections on
client_persistent_connections on
half_closed_clients off
